# 护龄 v3 — Python 依赖
# 安装: pip install -r requirements.txt
# 建议使用 Python 3.10

# ── 核心框架 ──
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.2.0         # YOLO11 人体检测 + 姿态估计

# ── 数据处理 ──
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# ── 机器学习评估 ──
scikit-learn>=1.3.0

# ── 可视化 ──
matplotlib>=3.7.0
tqdm>=4.65.0                # 进度条

# ── TensorBoard（训练监控） ──
tensorboard>=2.14.0

# ── 模型导出 ──
onnx>=1.15.0
onnxruntime>=1.16.0
onnxsim>=0.4.0              # ONNX 模型简化（可选）

# ── Web 前端服务 ──
flask>=3.0.0
flask-socketio>=5.3.0

# ── 可选：边缘部署库 ──
# onnxruntime-gpu           # GPU 加速推理
# tensorflow>=2.13.0        # TFLite 导出需要
# ncnn                       # NCNN 推理引擎（国产芯片常用）
