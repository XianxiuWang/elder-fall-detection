# 2026-05-09 工作日志

## 子会话：huling_model/inference.py 修复 - 13:08

子会话完成对 HuLing 模型推理脚本的多项修复：

### 完成的改动
1. **窗口中文乱码修复** — OpenCV imshow 标题/print 输出改为英文，避免 Windows ANSI 编码问题
2. **终端 R/Q 不响应修复** — 新增 `msvcrt.kbhit()` 双通道键盘监听，终端有焦点也能响应
3. **新增跌倒运动学规则校验** — 躯干角度、角速度、质心速度双重验证 + 连续5帧确认，防止坐姿误判为 fall
4. **数据集路径配置** — config.py 新增 EXTERNAL_DATASET_DIRS，dataset_loader.py 支持多目录
5. **删除概率柱状图** — 右侧 6 类概率条移除，只保留状态名 + 置信度 + 时间线
6. **隐藏面部关键点** — MediaPipe Pose landmark 0-10 不绘制，用 OpenCV 手动绘制 body landmarks 11-32
7. **修复绘制 bug** — `draw_landmarks` 不接受 list 传入 landmark_drawing_spec，改为纯 OpenCV 手动绘制

### 后续建议
- 根本解决坐姿误判需要重新训练模型（SisFall 数据）
